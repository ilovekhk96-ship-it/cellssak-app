# 셀싹 (Cell-ssak) — 프로젝트 인수인계 브리프

Claude.ai 채팅에서 프로토타입으로 만들던 "셀싹"을 Claude Code에서 실제
Firebase 연동 웹앱으로 새로 만들기 위한 정리 문서입니다. 아래 내용을 첫
Claude Code 대화에 붙여넣거나 읽어달라고 하면 이어서 작업할 수 있어요.

---

## 1. 이게 뭔가요

교회 셀(소그룹) 중보기도 명단 공유 앱. 셀원들이 기도 대상자 이름을
등록하고, 매일 "기도했어요"를 누르고, 믿음을 갖게 된 사람은 별도로
축하하는 용도. 셀 전체가 함께 키우는 나무 애니메이션이 메인 화면.

## 2. 지금까지 만든 프로토타입 (참고용)

Claude.ai 아티팩트로 만든 단일 파일 React 컴포넌트가 이 대화에 이미
공유되어 있어요 (`prayer-book.jsx`). 로그인·서버 없이 아티팩트 자체
저장소(`window.storage`)만 쓰는 버전이라 실제 서비스로는 못 쓰지만,
**화면 구조·디자인·인터랙션은 그대로 가져가면 됩니다.**

### 화면 구성
- 상단: 앱 이름("셀싹") + 셀 이름 표시
- 메인: 하늘~잔디 배경 위에 셀이 함께 키우는 나무
  - 나무 위 흰 말풍선: 기도 관련 성경구절 50개 중 랜덤 1개
  - 잎: 재귀적으로 생성한 가지 구조(트렁크→큰가지→잔가지) 위에, 나뭇잎
    모양 SVG 306장을 좌표 고정 배치. `score`(=기도자 수 × 경과일수)만큼
    앞에서부터 잎을 보여줘서 "한 장씩 자라는" 효과
  - 열매: 믿음열매 상태가 된 사람 수만큼 분홍 동그라미가 나무에 고정
    위치로 붙음
  - 하단: "OO일째 기도중" (첫 등록일로부터 경과일수) 진행 바 + 그 경계에
    🔥 이모지, 왼쪽 아래 하트 배지("오늘 기도한 인원수")
  - 왼쪽 아래 세로 아이콘: 추가하기 / 기도씨앗(N명) / 믿음열매(N명)
- 아이콘 클릭 시 나무 위로 모달(리스트) — 성경구절 헤더 + ㄱ~ㅎ/A-Z
  인덱스 스크롤 + 편집모드

### 데이터 모델 (entry 하나 = 기도 대상자 한 명)
```
{
  id, targetName, prayerName, relationship, note,
  status: 'seed' | 'fruit',
  prayerCount, lastPrayedDate, likeCount, createdAt
}
```
그 외: `cellName`, 셀 전체 통계는 entries에서 파생 계산.

### 디자인 시스템
- 폰트: 제목/큰 글씨 = 사용자가 업로드한 **Cafe24 Dongdong Light**
  (OFL 라이선스, 임베딩 허용 확인됨), 본문 = Google Fonts "Gowun Dodum"
- 색: 파스텔 하늘(#CFEFFB)~잔디(#A8DE9D) 그라데이션, 기도씨앗 세이지그린
  (#6FA66B), 믿음열매 코럴핑크(#F2678A), 하트 로즈핑크(#FF7F98, 검정 테두리)
- 나무: 재귀 branching 알고리즘으로 생성한 좌표를 정적 데이터로 박아둠
  (매번 랜덤 생성하지 않음 — 렌더링해서 눈으로 확인한 고정 데이터)

## 3. 새로 만들 실제 서비스에서 달라져야 하는 것

### 인증 & 구조
- **Firebase Authentication (Google 로그인)** 붙이기
- 계층 구조: **교회 → 셀 → 멤버**
  - 한 사람이 여러 셀에 속할 수도 있는지, 한 셀만 가능한지 정책 결정 필요
- **가입 승인제**: 새 사용자가 로그인 후 특정 셀에 "가입 신청"하면,
  하경님(또는 셀 리더)이 승인/거절할 수 있는 관리 화면 필요
  - Firestore에 `join_requests` 같은 컬렉션으로 대기열 관리 제안
- 셀별 데이터 완전 분리 (Firestore 보안 규칙으로 강제)

### 데이터가 정확해지는 부분
- 지금은 "기도자 이름"을 텍스트로만 입력받아서 같은 이름이면 같은
  사람으로 뭉치고 오타는 다른 사람으로 잡히는 문제가 있었음
- 로그인 기반이 되면 `prayerName` 대신 실제 유저 UID로 귀속시켜서
  "오늘 기도한 인원", "참여 인원" 같은 통계가 정확해짐

## 4. 제안하는 Firestore 구조 (초안, 같이 다듬으면 됨)

```
churches/{churchId}
  name, createdAt

churches/{churchId}/cells/{cellId}
  name, leaderUid, createdAt

churches/{churchId}/cells/{cellId}/entries/{entryId}
  targetName, prayerUid, prayerName(표시용), relationship, note,
  status, prayerCount, lastPrayedDate, likeCount, createdAt

churches/{churchId}/cells/{cellId}/joinRequests/{uid}
  displayName, requestedAt, status: 'pending' | 'approved' | 'rejected'

churches/{churchId}/cells/{cellId}/members/{uid}
  displayName, joinedAt, role: 'leader' | 'member'

users/{uid}
  displayName, email, cellIds: [...]
```

## 5. 기술 스택 제안

- **프론트엔드**: React + Vite (또는 Next.js — 배포·라우팅 편의에 따라
  선택), Tailwind CSS
- **인증/DB**: Firebase Auth (Google 로그인) + Firestore
- **배포**: Firebase Hosting 또는 Vercel
- **버전 관리**: GitHub 저장소, Claude Code에서 커밋

## 6. 다음 단계 제안 (Claude Code에서 시작할 때)

1. 로컬에 Vite + React + Tailwind 프로젝트 스캐폴딩
2. Firebase 프로젝트 생성은 하경님이 콘솔에서 직접 (Claude Code가
   안내는 가능) → Auth(Google 활성화), Firestore 생성
3. `prayer-book.jsx`의 UI를 여러 컴포넌트 파일로 쪼개서 이식
   (TreeScene, ListModal, EntryRow, EntrySheet 등 이미 컴포넌트 단위로
   나뉘어 있어서 그대로 파일 분리하면 됨)
4. `window.storage` 호출부를 Firestore 호출로 교체
5. 로그인 화면 + 교회/셀 선택 화면 + 가입 승인 대기 화면을 앱 진입
   흐름 맨 앞에 추가 (로그인 → 교회 선택 → 셀 선택 → [승인 대기 or
   메인 화면])
6. 관리자(하경님) 전용 승인 화면 추가
