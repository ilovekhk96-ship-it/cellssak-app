# 셀싹 (Cell-ssak) — cellssak-app 프로젝트 기획서

> 이 문서는 `cellssak-app` 프로젝트(원래 셀싹 앱과는 별도의 Firebase/GitHub
> 프로젝트, 실사 이미지 테마 트랙)의 현재 상태와 앞으로의 계획을 정리한
> 문서예요. 새 Claude Code 대화나 다른 기기에서 이어서 작업할 때 이 문서를
> 먼저 읽어달라고 하면 맥락을 빠르게 따라잡을 수 있어요.

---

## 1. 이게 뭔가요

교회 셀(소그룹) 중보기도 명단 공유 앱. 셀원들이 기도 대상자 이름을
등록하고, 매일 "기도했어요"를 누르고, 응답받았다고 믿게 된 대상은
"믿음열매"로 전환한다. 셀 전체가 함께 키우는 기도나무가 메인 화면이고,
개인 전용 기도나무 화면도 따로 있다(같은 화면 구조를 재사용).

## 2. 지금 실제로 되어 있는 것 (2026-09 기준)

### 인증 & 구조
- Firebase Authentication(Google 로그인) 연동 완료
- 계층 구조: 교회 → 셀 → 멤버, 가입 승인제(셀 리더가 승인/거절)
- 슈퍼관리자 화면 별도 존재(교회/셀/멤버 강제 관리)
- Firestore 보안 규칙으로 셀별 데이터 분리, 필드 단위 diff 기반 권한 제어

### 기도 항목 (entries)
- 개인용(`users/{uid}/prayerRequests`)과 셀용
  (`churches/.../cells/.../entries`) 스키마를 최대한 통일해서 같은
  컴포넌트(EntryRow/ListModal/EntrySheet)를 재사용
- "나의기도"(이름 없음) / "중보기도"(이름+관계) 탭, 같은 추가 흐름
- 저장 위치는 **지금 보고 있는 화면 기준**(개인 화면 → 항상 개인,
  셀 화면 → 항상 셀), `복사하기`/`공유하기`로 반대편에 **연동된 사본**을
  만들 수 있고, 연동된 항목은 상태·내용이 양쪽에 동기화됨
- 믿음열매 전환은 작성자·리더만 가능(기도했어요/좋아요는 셀원 전체 공개)
- 알림 종: 다른 셀원의 믿음열매 전환, "기도할 시간이에요", "황금
  나뭇잎이 자랐어요"(주간 보너스) — 안 읽음 빨간 점 표시

### 성장(나무) 메커니즘
- `score`(누적 기도 일수, 개인은 하루 최대 1장) 만큼 나뭇잎이 절차적으로
  생성됨, 매주(7일)마다 그 사람 몫으로 1장은 황금잎
- 개인 나무와 셀 나무의 "기도했어요"는 서로의 성장에도 함께 반영됨
- 열매(믿음열매) 개수만큼 열매 이미지가 나무에 붙음
- 나무·잎·열매 모두 **실사 이미지로 교체 완료** (아래 "실사 테마" 참고)

### 실사 테마 (지금 유일하게 완성된 테마 — 나중에 "테마 1: 리얼버전"이 됨)
- 배경: 하늘~들판 사진(`bg-field.jpg`), 로딩화면도 나무 캐노피 사진
- 나무: 실사 나무 사진(`tree.png`, 투명 배경) 위에 절차적으로 잎/열매를
  얹는 구조
  - **핵심 기법**: 잎/열매가 달릴 수 있는 자리를 벡터 좌표로 대충
    흉내내지 않고, `tree.png`를 실제로 픽셀 단위(4~5px 격자)로 스캔해서
    "알파가 불투명하면서 주변에 빈틈이 충분히 있는"(=잔가지, 몸통 아님)
    지점만 좌표로 뽑아 하드코딩(`app/src/data/treeData.js`의
    `BRANCH_PIXELS`). 잎의 방향(줄기가 어느 쪽을 향할지)도 그 지점
    주변 실제 가지 픽셀의 무게중심 방향을 계산해서 정함 — 전역 중심
    기준 방향보다 훨씬 자연스러움
  - 잎 2종(leaf-1/leaf-2, 사진마다 줄기 위치가 달라 흔들림 축도 따로
    지정), 구름 1종(cloud-3, 좌우 반전으로 변형), 잔디 1종(grass-1)을
    같은 방식(투명 PNG를 절차적으로 여러 장 배치)으로 재사용
  - 잎+열매는 "자란 순서"를 하나로 합쳐서(열매는 그 시점까지의 잎
    개수 사이에 비례 배치했다고 추정) 나중에 자란 잎이 먼저 있던
    열매 위에 그려지도록 함
  - 애니메이션: 잎/잔디 모두 "쏴아(한쪽으로 훅 기울었다 제자리) → 잠깐
    흔들 → 쏴아 → 제자리" 리듬의 바람 애니메이션. 잎은 왼쪽→오른쪽
    순서로 훑고 지나가는 느낌(x좌표 기준 애니메이션 딜레이). 구름은
    `transform: translateX`(vw 단위)로 화면을 매끄럽게 가로지름 —
    `left` 속성 애니메이션은 매 프레임 레이아웃 재계산이 필요해서
    느릴 때 끊겨 보였던 문제가 있었음
  - 나무 그림자: 햇빛 방향(사진 속 왼쪽 위)에 맞춰 발치에서 오른쪽
    아래로 늘어지는 두 겹 + 블러 그림자
- 나무 이동 버튼(개인↔셀): 잎 사진 아이콘 + 텍스트에 흰 테두리(사진
  알파를 따라가는 drop-shadow 여러 겹) — 글씨는 굵게+테두리로, 배경
  흰 원판은 쓰지 않음(하경님이 "쨔치다"고 하심)

### 아직 안 만든 것 / 임시 상태
- **타이머**: 왼쪽 아래 버튼만 있고 "준비 중이에요" 토스트만 뜸. 지금
  다시 설계 논의 시작함(§5 참고)
- **캘린더(기도 잔디)**: 버튼만 있고 미구현. 이름은 "기도 잔디"로
  확정(GitHub 컨트리뷰션 잔디 스타일 히트맵)
- PersonalHome.jsx에 성장 단계 미리보기용 임시 디버그 버튼
  (7/30/100/300/1000/3000 잎 수 눌러보기)이 남아있음 — 확인 다 끝나면
  제거 예정

## 3. 디자인 시스템

- 폰트: 제목 = Cafe24 Dongdong Light, 본문 = Gowun Dodum
- 색: 잉크 `#4A3B3F`, 기도씨앗 세이지그린 `#6FA66B`, 믿음열매 계열
  핑크/포도색, 흰 카드 `#FFFDF9`
- UI 패턴: 팝오버/시트는 전부 화면 중앙에 뜨는 모달(둥근 모서리,
  `#00000040` 배경 딤)로 통일. 점 세개(⋮) 메뉴가 항목별 수정/삭제/
  공유를 담당(편집 모드 토글 없앰, 스와이프 대신 ⋮ 선택 — 다양한
  연령대 교인들 대상이라 발견성·안전성 우선)
- z-index: TreeMoveButton(5) < ListModal(10) < 시트류(20) < 확인창/
  ⋮메뉴/네비 팝오버(30) < 슈퍼관리자 톱니(40)
- **주의**: `position` 없는 정적 요소는 `position:absolute` 형제
  요소한테 항상 가려질 수 있음(헤더 줄, 말풍선에서 실제로 겪은 버그) —
  구름/장식 같은 절대배치 레이어 근처 UI는 꼭 `position:relative` +
  z-index 명시하기

## 4. 지금 논의 중 / 예정된 큰 기능들

### 4-1. 테마 + 나무 종류 + 계절 시스템 (제일 큰 예정 작업)
- **테마 5종**: ① 지금의 리얼(사진) 버전 ② 제일 처음 만든 벡터 그림
  버전(git 히스토리에 그대로 남아있음, 거의 그대로 되살릴 수 있음)
  ③ 픽셀 버전 ④ 일러스트 버전 ⑤ 귀여운 캐릭터 버전
  - 테마가 바뀌는 범위: 나무·배경·구름·버튼들. 앱 이름/프로필/알림
    부분은 테마 상관없이 고정
  - 처음 로그인할 때 테마를 고르지만 영구 고정은 아니고, 나중에
    토큰(또는 광고 시청)으로 변경 가능
- **테마별 나무 종류 선택**: 기본(리얼) 버전은 나무 선택 없음. 다른
  테마에서는 은행나무(갈색 나무+은행잎), 플라타너스(흰빛 섞인
  나무+지금 쓰는 잎모양), 바오밥나무(특별한 수형+기존 손그림 잎),
  버드나무 — 하경님이 "느타리나무"라고 하셨는데 축 늘어진 가지+작고
  귀여운 잎이라는 설명으로 볼 때 버드나무를 말씀하신 것으로 보임,
  확인 필요 — 등 다양하게. **한 번 고른 나무 종류는 못 바꿈**(테마/나무
  변경 자체가 유료 액션)
- **계절별 잎/열매**: 3~5월 벚나무 느낌 분홍잎+앵두, 6~8월
  플라타너스 초록잎+포도, 9~11월 단풍(노랑/주황)+감, 12~2월
  눈 쌓인 초록나무+솔방울. 나무 "종류"(모양)는 고정이어도 잎 색깔과
  열매 종류는 계절 따라 바뀜. **테마별로도 다른 잎/열매 그림을
  쓸 예정**(하경님이 지피티로 직접 생성해서 전달 예정)
- **결정된 것**: 결제는 실제 돈 대신 **광고 시청으로 토큰 획득**부터
  시작(실제 인앱결제는 나중에, 웹앱이라 스토어 결제 자체가 안 붙고
  Stripe 등 별도 결제 연동은 사업자등록·세금 이슈가 있어서 보류)
- **작업 순서 합의**: 남은 핵심 기능(타이머, 기도 잔디 등) 먼저 다
  정비 → 그동안 하경님이 테마별 그림 준비 → 그림 다 모이면 테마
  시스템을 한 번에 제대로 설계
- 나중에 장식품(크리스마스, 여름 피서 등 카테고리별)도 토큰/광고로
  구매하는 상점 추가 예정 — 제일 나중 순위

### 4-2. 타이머 (지금 다시 설계 논의 시작)
- 이름 후보: "기도 타이머"(가안, 아직 미확정)
- 화면 컨셉: 지금 실사 배경(하늘+들판)과 이어지는 느낌으로, 그 위에
  원형 링 타이머(칸/헤드스페이스 스타일: 큰 도넛형 진행 링 + 중앙에
  MM:SS)만 얹는 방향으로 논의 중
- 시간 선택(5/10/15분 등) → 시작 시 링이 채워지며 카운트다운 → 완료
  화면, 이 흐름으로 잠정 합의했으나 세부는 미확정
- 배경음악은 하경님이 Suno(AI 음악 생성)로 직접 만들어서 저작권 문제
  피할 예정 — 음원 완성 여부 확인 필요

### 4-3. 기도 잔디 (캘린더/히트맵)
- 이름 확정: **기도 잔디**
- GitHub 컨트리뷰션 그래프 스타일 히트맵(요일×주 격자, 활동량에 따라
  색 진하기 다름)
- 색·테마가 나중에 바뀌어도 "잔디"라는 이름 자체는 GitHub처럼 관용적
  별칭으로 그대로 써도 괜찮다고 합의(색과 무관하게 "이런 스타일의
  히트맵"을 가리키는 고유명사처럼)
- 순서상 테마 시스템보다 먼저 착수하기로 함(§4-1 참고)

## 5. Firestore 구조 (실제 구현 기준, 초안에서 갱신)

```
churches/{churchId}
  name, createdAt

churches/{churchId}/cells/{cellId}
  name, leaderUid, createdAt

churches/{churchId}/cells/{cellId}/entries/{entryId}
  authorUid, targetName, prayerName(표시용), relationship, note,
  type: 'mine' | 'intercession', status: 'seed' | 'fruit',
  linkedRef: { uid, reqId } | null,
  prayerCount, lastPrayedDate, likeCount, createdAt

churches/{churchId}/cells/{cellId}/joinRequests/{uid}
  displayName, requestedAt, status

churches/{churchId}/cells/{cellId}/members/{uid}
  displayName, joinedAt, role: 'leader' | 'member'

churches/{churchId}/cells/{cellId}/dailyPrayers/{date}
  { uid: [...] }  // arrayUnion

churches/{churchId}/cells/{cellId}/activity/{activityId}
  targetName, prayerName, actorUid, entryType, createdAt  // 알림용

users/{uid}
  displayName, nickname, photoURL, email, activeCell,
  pendingRequest, leaderNotifiedFor

users/{uid}/prayerRequests/{reqId}
  targetName, prayerName, relationship, note, type, status,
  linkedRef: { churchId, cellId, entryId } | null,
  prayerCount, lastPrayedDate, likeCount, createdAt

users/{uid}/dailyActivity/{date}
  { active: true }
```

## 6. 기술 스택 / 배포

- 프론트엔드: React + Vite, Tailwind CSS
- 인증/DB: Firebase Auth(Google) + Firestore, 이미지 업로드는 Storage
- 배포: Firebase Hosting (`cellssak-app` 프로젝트)
- 버전 관리: GitHub(`ilovekhk96-ship-it/cellssak-app`), Claude Code에서
  커밋+푸시까지 진행
- **배포 루틴**: `app/` 폴더에서 `npm run build` → 검증 →
  `npx firebase-tools deploy --only hosting --project cellssak-app`
  (rules 바뀌면 `--only firestore:rules`도) → git add/commit/push
- **다른 기기에서 이어서 작업하기**: 같은 계정으로 Claude Code
  로그인 + (OneDrive 자동 동기화 또는 `git clone`으로 코드 받기) +
  Node.js/Git 설치. `npx firebase-tools`라 전역 설치 없이 배포 가능,
  필요시 브라우저 로그인 한 번 더 뜸

## 7. 다음에 할 일 (우선순위 순)

1. 타이머 화면 설계 확정(이름, 화면 흐름, 음원 유무) → 구현
2. 기도 잔디(히트맵) 구현
3. PersonalHome.jsx의 임시 미리보기 디버그 버튼 제거
4. 테마별 그림(하경님이 지피티로 준비) 다 모이면 테마 시스템 설계·구현
5. 그 다음: 토큰(광고 기반) 시스템 → 장식품 상점
